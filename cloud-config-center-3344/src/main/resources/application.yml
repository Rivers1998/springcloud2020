server:
  port: 3344

spring:
  application:
    name: cloud-config-center # 注册金eureka服务器的微服务名
  cloud:
    config:
      server:
        git:
          uri: git@github.com/Rivers1998/springcloud-config.git # github上git仓库的名字
          search-paths:
            - spring-cloud-config # 搜索目录
      label: master # 读取分支

eureka:
  client:
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/,http://eureka7002.com:7002/eureka/
